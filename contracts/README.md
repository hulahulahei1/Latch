# LATCH Contracts

This folder contains a minimal Solidity scaffold for LATCH.

It is intentionally dependency-light and protocol-first.

Build:

```bash
forge build
```

Important: `MockProofVerifier` is not a production verifier. Replace it with a real verifier adapter before any live deployment.
